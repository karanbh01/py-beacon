# Indices

Defining an index and calculating it: `IndexDefinition` holds the rules, the methodology module has the selection rules and weighting schemes, `IndexCalculator` walks the calendar's sessions, and trading calendars live in `beacon.index.schedule`. See [Methodology](../concepts/methodology.md).

::: beacon.index
